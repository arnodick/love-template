local function make(a)

end

local function control(a,gs)

end

local function hitground(a)

end

local function draw(a)

end

return
{
	make = make,
	control = control,
	hitground = hitground,
	draw = draw,
}